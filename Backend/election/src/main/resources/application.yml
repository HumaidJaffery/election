spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/election
#   pretend you can not see this pls
    password: billybobjoe2152
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: true